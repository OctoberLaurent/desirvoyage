{% extends 'base.html.twig' %}

{% block title %}Login{% endblock %}

{% block content %}
    <form method="post">
        <div class="login">
            <div class="container">
                <h1 class="pad">{% trans %} To log in {% endtrans %}</h1>
                <div class="col s9">
                    <div class="input-field col">
                        <i class="material-icons prefix"></i>
                        <input type="email" value="{{ last_username }}" id="email-input" class="autocomplete"
                               name="email">
                        <label for="email-input">Email</label>
                    </div>
                </div>
                <div class="col s3">
                    <div class="input-field col">
                        <i class="material-icons prefix"></i>
                        <input type="password" id="password-input" class="password" name="password">
                        <label for="password">{% trans %} Password {% endtrans %}</label>
                    </div>
                </div>
                <input type="hidden" name="_csrf_token"
                       value="{{ csrf_token('authenticate') }}">

                <button type="submit" class="btn btn-large" id="submit">{% trans %} Login {% endtrans %}</button>

                <div id="forgotPasswordContainer">
                    <a id="forgetPasswordLink" href="{{ path("RenewPassword") }}">{% trans %} Forgot your password {% endtrans %} ?</a>
                </div>
            </div>
        </div>
    </form>


{% endblock %}
