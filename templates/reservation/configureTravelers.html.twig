{% extends 'base.html.twig' %}

{% block title %}Reservation{% endblock %}

{% block content %}
    <h1 class="center-align mt-1">Qui embarque ?</h1>
    <div class="container">
        <div class="divmg mt-1">
            <img class="img-100pc" src="/images/polynesia.jpg" alt="Options_travel">
        </div>
        <p>Pour vous ajouter au voyage, appuyez simplement sur le bouton m'ajouter.</p>
        <table>
            <thead>
            <tr>
                <th>Nom</th>
                <th>Prénom</th>
                <th>Email</th>
                <th>Anniversaire</th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            <tr>
                <td>{{ app.user.lastname }}</td>
                <td>{{ app.user.firstname }}</td>
                <td>{{ app.user.email }}</td>
                <td>{{ app.user.birthday | date("d/m/Y") }}</td>
                <td><button class="waves-effect waves-light btn green" id="add-user-in-traveler">M'ajouter</button></td>
            </tr>
            </tbody>
        </table>
        <!--separation line -->
        <h2 class="center-align pad">Les voyageurs </h2>
        <hr class="pink">
        <div class="row">
            <div class="col l12">
                {{ form_start( form ) }}
                <ul id="traveler-fields-list"
                    data-prototype="{{ form_widget(form.travelers.vars.prototype)|e }}"
                    data-widget-tags="{{ '<li></li>'|e }}"
                    data-widget-counter="{{ form.travelers|length }}">
                    {% for t in form.travelers %}
                        <li>
                            {{ form_row(t.firstname) }}
                            {{ form_row(t.lastname) }}
                            {{ form_row(t.email) }}
                            {{ form_row(t.birthday) }}
                        </li>
                    {% endfor %}
                </ul>
                <button type="button"
                        class="add-another-collection-widget btn btn-primary indigo"
                        data-list-selector="#traveler-fields-list"
                        id="add-traveler">Add another traveler
                </button>
                <br>
                <div class="right-align">
                    <a class="btn waves-effect waves-light" href="{{ path('reservation_configure', {'id': reservation.stays[0].id}) }}">Preview
                        <i class="material-icons left">keyboard_arrow_left</i>
                    </a>
                    <button type="submit" class="waves-effect waves-light btn" name="action">Next
                        <i class="material-icons right">keyboard_arrow_right</i></button>
                </div>
                {{ form_end( form ) }}
            </div>
        </div>
    </div>
{% endblock %}
{% block javascripts %}{{ parent() }}{{ encore_entry_script_tags('usercollection') }}{% endblock %}
